"""
Pydantic request/response schemas live here (one file per resource, e.g.
business.py, user.py, review.py), mirroring app/models/.

Empty for now — filled in alongside each module's router (Module 2+).
"""
